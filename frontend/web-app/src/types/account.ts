/**
 * Represents one account in the system.
 */
export interface Account {
    firstName: string;
    lastName: string;
    personNumber: string;
    email: string;
    username: string;
    password: string;
}
